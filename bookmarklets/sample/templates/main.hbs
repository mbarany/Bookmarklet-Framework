<div class="bookmarklet-demo">
    <a href="#" class="close">&#10006;</a>
    <div class="container">
        <h1>
            {{title}}
        </h1>

        <ul>
            {{#each list}}
                <li><input type="checkbox"> {{login}}</li>
            {{/each}}
        </ul>
    </div>
</div>
